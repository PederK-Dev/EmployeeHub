# Base URL of the EmployeeHub API, including the /api prefix.
# Must match the address the backend prints on startup (see Properties/launchSettings.json).
# Copy this file to `.env` before running `npm run dev`.
VITE_API_URL=http://localhost:5023/api
